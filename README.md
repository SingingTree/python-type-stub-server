A little project messing around with generating and serving Python type stubs.

This project uses [just](https://github.com/casey/just) as its command runner. Running `just` will describe
the available commands.